import * as PropTypes from "prop-types";
export declare const defaultProps: ({ propTypes, defaults, keys, }: {
    propTypes: PropTypes.ValidationMap<any>;
    defaults?: any;
    keys?: string[] | readonly string[];
}) => DefaultProps;
